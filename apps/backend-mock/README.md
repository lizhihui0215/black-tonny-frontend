# @vben/backend-mock

Business mock service for `black-tonny-frontend`, aligned to the official `vue-vben-admin v5` `apps/backend-mock` pattern.

It provides the repo-owned mock routes that frontend development and local frontend/backend parallel work rely on before the sibling backend takes over the same formal `/api/*` paths.
